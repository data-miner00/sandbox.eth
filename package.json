{
  "name": "sandbox.eth",
  "version": "0.0.1",
  "license": "MIT",
  "scripts": {
    "build:hardhat": "nx build hardhat",
    "build:truffle": "nx build truffle",
    "build:docs": "nx build docs",
    "serve:docs": "nx serve docs",
    "test:hardhat": "nx test hardhat",
    "test:truffle": "nx test truffle",
    "build:affected": "nx affected --target=build",
    "graph": "nx graph",
    "print:affected": "nx print-affected --select=projects"
  },
  "devDependencies": {
    "@nrwl/nx-cloud": "latest",
    "nx": "15.5.1",
    "prettier": "^2.8.2"
  }
}
